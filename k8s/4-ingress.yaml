apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lr-app-ingress
  namespace: lr-app
  annotations:
    kubernetes.io/ingress.class: "nginx"
    
spec:
  rules:
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lr-app-svc
                port:
                  number: 80
